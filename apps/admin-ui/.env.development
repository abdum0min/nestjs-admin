# Development only. The SPA runs on the Vite dev server while the admin API is
# served by the application on port 3000, so requests go through the dev proxy
# defined in vite.config.ts rather than to a same-origin `/admin`.
VITE_ADMIN_API_BASE=/__admin-api
