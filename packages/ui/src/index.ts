/**
 * `@nest-admin/ui` - reusable React components for the admin.
 *
 * Nothing is implemented yet. Components (Button, Input, Table, Modal, Form,
 * Select, Pagination, ...) will be added as the admin UI needs them - driven
 * by real screens, not by filling in a component-library checklist.
 *
 * React is a peer dependency: the admin application owns the React instance,
 * and two copies would break hooks.
 */

export {}
