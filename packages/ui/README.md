# @nest-admin/ui

Reusable React components. Internal package — consumed by `apps/admin-ui`.

**Nothing is implemented yet.**

Components will be added when a real admin screen needs them, not up front.
`react` and `react-dom` are peer dependencies — two React copies break hooks.
